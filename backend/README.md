# ShopSphere Backend

Express/Node.js backend for ShopSphere

## Required environment variables


## Runtime
The repository root starts the backend with `npm start`. The backend serves the production frontend from `frontend/build`.
