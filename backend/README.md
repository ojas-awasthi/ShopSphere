# ShopSphere Backend

Express/Node.js backend for ShopSphere, adapted from the original Cricket Weapon open-source project.

## Required environment variables
See `config/config.env.example`. Do not commit real secrets.

## Runtime
The repository root starts the backend with `npm start`. The backend serves the production frontend from `frontend/build`.
